# Loaded automatically by Vite in `npm run dev` (development mode only).
# Put secrets in .env.local (gitignored) — e.g. VITE_BASE44_API_KEY — see .env.example

VITE_BASE44_APP_ID=69a35c4a7320c1762a3e2af0
VITE_BASE44_APP_BASE_URL=https://smartqueue.base44.app
